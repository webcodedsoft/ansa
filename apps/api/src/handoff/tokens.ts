/** Holds the summaries waiting to be spoken to whoever answers a transfer. */
export const WHISPER_REGISTRY = Symbol("WHISPER_REGISTRY");

/** Where a transfer goes, or null when nothing is configured. */
export const HANDOFF_DESTINATION = Symbol("HANDOFF_DESTINATION");
